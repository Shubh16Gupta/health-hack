{
  "name": "ayuroot",
  "version": "1.0.0",
  "description": "Ayurveda Chatbot - Frontend and Backend",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "start": "cd backend && npm start"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": "24.x"
  }
}
